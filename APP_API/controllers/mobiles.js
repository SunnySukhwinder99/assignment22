const getMobiles = (req,res) => {
    res
    .status(200)
    .json({'status': 'success'});
};

const createMobiles = (req,res) => {
    res
    .status(200)
    .json({'status': 'success'});
};

const getSingleMobile = (req,res) => {
    res
    .status(200)
    .json({'status': 'success'});
};

const updateMobiles = (req,res) => {
    res
    .status(200)
    .json({'status': 'success'});
};

const deleteMobiles = (req,res) => {
    res
    .status(200)
    .json({'status': 'success'});
};

module.exports = {
    getMobiles,
    createMobiles,
    getSingleMobile,
    updateMobiles,
    deleteMobiles
};